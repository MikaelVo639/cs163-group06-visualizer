# AI Usage Notes

## 1. Dropdown Widget Implementation

- **Tool name, version, and platform:** Gemini. Gemini 3.1, Google, *gemini.google.com*.
- **Access time (date and hour):** March 27, 2026, 21:00 - 22:30.
- **Prompts used:** 
  1. *"How to correctly handle mouse click events and bounding box collisions using SFML 3.x mapPixelToCoords?"*
  2. *"What is the equivalent of sf::Text's default constructor in SFML 3.0.x API? I am getting a 'candidate expects 1 argument' compiling error."*
  3. *"Write a C++ snippet to draw an exact isosceles downward-pointing triangle using sf::ConvexShape."*

- **Purpose of use:** Used to resolve syntax errors caused by breaking API changes in the newly released SFML 3.x, and to assist with the mathematical coordinate geometry required for rendering custom UI shapes (dropdown arrows).

- **Which content was generated by AI:** The AI provided the vertex coordinates to render the triangle arrow (`sf::ConvexShape`), the mathematical snippet for converting mouse coordinates to world coordinates (`window.mapPixelToCoords`), and the structural syntax fix for initializing `sf::Text` components via C++ constructor initializer lists.

- **Which content was done independently and how the student edited or validated it:** 
  - **Architecture & Logic (Student):** The student independently designed the [Dropdown](file:///d:/APCS%282025-2026%29/HK2/Data_Structures/cs163-group06-visualizer/include/UI/Widgets/Dropdown.hpp#14-15) class architecture and seamlessly integrated it into the [App](file:///d:/APCS%282025-2026%29/HK2/Data_Structures/cs163-group06-visualizer/include/Core/App.hpp#11-29)'s main event loop. The student wrote the entire state machine controlling the dropdown interactions (toggling the `isDropped` flag, tracking `selectedIndex`, and handling UI interaction bounds). 
  - **Styling & Validation (Student):** The student manually customized the AI's mathematical snippets to match the project's layout dimensions. Furthermore, the student designed the Dark Theme palette (colors, outlines, and paddings) to ensure the widget seamlessly matched the other existing UI components ([Button.hpp](file:///d:/APCS%282025-2026%29/HK2/Data_Structures/cs163-group06-visualizer/include/UI/Widgets/Button.hpp)). Finally, the component was extensively compiled and tested on-screen by the student.

---

## 2. LinkedListScreen UI Optimization & Refactoring

- **Tool name, version, and platform:** Gemini 3.1 Pro (High), Google, built-in IDE agent.
- **Access time (date and hour):** April 3, 2026, 10:00 - 14:30.
- **Prompts used:** 
  1. *"Optimize LinkedList visualizer UI: implement a contextual UI to only display necessary input fields based on user selection."*
  2. *"Refactor memory management in LinkedListScreen to use smart pointers (std::unique_ptr) for robust resource handling of dynamic UI elements."*
  3. *"Fix layout and positioning logic when dropdown menus are expanded to prevent UI overlap with text and buttons."*

- **Purpose of use:** Used to refactor the `LinkedListScreen` state component, specifically migrating from raw pointers to modern C++ `std::unique_ptr` for dynamic input fields, as well as fixing complex dynamic layout calculations (positioning offsets, paddings) when contextual menus expand or contract.

- **Which content was generated by AI:** The AI provided the boilerplates for `std::unique_ptr` transformations, wrote the algorithmic positioning logic (`setPosition()`, dynamic Y-offset calculations) to prevent overlaps, and suggested a context-based approach for hiding/showing specific input fields depending on the selected operation variant in the dropdown.

- **Which content was done independently and how the student edited or validated it:**
  - **Requirements & Verification (Student):** The student defined the exact behavior of the contextual UI (e.g., determining which specific operation variants require an Index vs. a Value input field) and provided the base logic file (`LinkedListScreen.cpp`) for the agent to optimize.
  - **Integration & Refinement (Student):** The student steered the implementation, requested explicit corrections when elements overlapped or layout rules broke, and manually validated the interactions within the main visualizer loop to ensure the user experience was smooth and consistent with the project's overall design aesthetics.

---

### Example Extract (For Section Inclusion)

- **Gemini.** *Gemini 3.1 Pro, Google*, accessed March 27 and April 3, 2026. 
  - **Prompts:** 
    - *"How to handle bounding box collisions using SFML 3.x mapPixelToCoords"* 
    - *"Refactor memory management in LinkedListScreen to use std::unique_ptr"*
  - **Purpose:** Used to assist the implementation of the custom Dropdown menu and optimize contextual layouts in `LinkedListScreen`. 
  - **AI Contribution:** Provided API syntax fixes for SFML 3.x, geometric vertex math, modern C++ smart pointer transformations, and logic for dynamic widget positioning. 
  - **Student Contribution:** Designed component architectures, defined contextual logic per operation variant, handled the main state machine loops, integrated the AI snippets seamlessly into the codebase, and conducted manual on-screen testing.
