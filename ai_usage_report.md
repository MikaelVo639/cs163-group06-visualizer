# AI Usage Notes

## 1. Dropdown Widget Implementation

- **Tool name, version, and platform:** Gemini. Gemini 3.1, Google, *gemini.google.com*.
- **Access time (date and hour):** March 27, 2026, 21:00 - 22:30.
- **Prompts used:** 
  1. *"How to correctly handle mouse click events and bounding box collisions using SFML 3.x mapPixelToCoords?"*
  2. *"What is the equivalent of sf::Text's default constructor in SFML 3.0.x API? I am getting a 'candidate expects 1 argument' compiling error."*
  3. *"Write a C++ snippet to draw an exact isosceles downward-pointing triangle using sf::ConvexShape."*

- **Purpose of use:** Used to resolve syntax errors caused by breaking API changes in the newly released SFML 3.x, and to assist with the mathematical coordinate geometry required for rendering custom UI shapes (dropdown arrows).

- **Which content was generated by AI:** The AI provided the vertex coordinates to render the triangle arrow (`sf::ConvexShape`), the mathematical snippet for converting mouse coordinates to world coordinates (`window.mapPixelToCoords`), and the structural syntax fix for initializing `sf::Text` components via C++ constructor initializer lists.

- **Which content was done independently and how the student edited or validated it:** 
  - **Architecture & Logic (Student):** The student independently designed the Dropdown class architecture (Dropdown.hpp) and seamlessly integrated it into the application's main event loop (App.hpp). The student wrote the entire state machine controlling the dropdown interactions (toggling the `isDropped` flag, tracking `selectedIndex`, and handling UI interaction bounds). 
  - **Styling & Validation (Student):** The student manually customized the AI's mathematical snippets to match the project's layout dimensions. Furthermore, the student designed the Dark Theme palette (colors, outlines, and paddings) to ensure the widget seamlessly matched the other existing UI components (Button.hpp). Finally, the component was extensively compiled and tested on-screen by the student.

---

## 2. LinkedListScreen UI Optimization & Refactoring

- **Tool name, version, and platform:** Gemini 3.1 Pro (High), Google, built-in IDE agent.
- **Access time (date and hour):** April 3, 2026, 10:00 - 14:30.
- **Prompts used:** 
  1. *"Optimize LinkedList visualizer UI: implement a contextual UI to only display necessary input fields based on user selection."*
  2. *"Refactor memory management in LinkedListScreen to use smart pointers (std::unique_ptr) for robust resource handling of dynamic UI elements."*
  3. *"Fix layout and positioning logic when dropdown menus are expanded to prevent UI overlap with text and buttons."*

- **Purpose of use:** Used to refactor the `LinkedListScreen` state component, specifically migrating from raw pointers to modern C++ `std::unique_ptr` for dynamic input fields, as well as fixing complex dynamic layout calculations (positioning offsets, paddings) when contextual menus expand or contract.

- **Which content was generated by AI:** The AI provided the boilerplates for `std::unique_ptr` transformations, wrote the algorithmic positioning logic (`setPosition()`, dynamic Y-offset calculations) to prevent overlaps, and suggested a context-based approach for hiding/showing specific input fields depending on the selected operation variant in the dropdown.

- **Which content was done independently and how the student edited or validated it:**
  - **Requirements & Verification (Student):** The student defined the exact behavior of the contextual UI (e.g., determining which specific operation variants require an Index vs. a Value input field) and provided the base logic file (`LinkedListScreen.cpp`) for the agent to optimize.
  - **Integration & Refinement (Student):** The student steered the implementation, requested explicit corrections when elements overlapped or layout rules broke, and manually validated the interactions within the main visualizer loop to ensure the user experience was smooth and consistent with the project's overall design aesthetics.

---

## 3. Pseudocode Animation System Architecture & Refactoring

- **Tool name, version, and platform:** Gemini 1.5 Pro (Custom Agent), Google, built-in IDE agent (Antigravity).
- **Access time (date and hour):** April 14, 2026, 14:00 - April 15, 2026, 08:00.
- **Prompts used:** 
  1. *"Refactor the pseudocode display system to use labels instead of magic line numbers to improve maintainability."*
  2. *"Design a fluent builder API (AnimStepBuilder) in C++ to allow human-readable construction of animation sequences."*
  3. *"Help me bulk-convert these raw pseudocode strings into structured PseudoCodeDef containers with appropriate labels."*

- **Purpose of use:** Used to modernize the project's core animation logic by decoupling the high-level algorithm steps from the low-level UI rendering indices. This involved creating a label-based lookup system and a sequence-based animation builder.

- **Which content was generated by AI:** The AI proposed the structured data model (`PseudoCodeDef`, `PseudoCodeLine`), implemented the skeleton for the `AnimStepBuilder` fluent interface, and assisted in generating the initial labeled datasets for LinkedList and Trie algorithms in `PseudoCodeData.hpp`.

- **Which content was done independently and how the student edited or validated it:**
  - **Logic Mapping (Student):** The student defined the exact logical breakpoints for every algorithm (e.g., determining where a "bounds_check" ends and where "init_curr" begins) to ensure educational clarity.
  - **Controller Integration (Student):** The student performed the heavy lifting of refactoring the LinkedListController.cpp to replace legacy animation code with the new builder-based sequences. The student manually synchronized the visual node highlights with the specific pseudocode labels.
  - **Architecture Validation (Student):** The student validated the architectural change by extending the system to support the Trie visualizer, ensuring the refactored system was truly modular and reusable.

---

## Overall AI Contribution Summary

Following an exhaustive review of the codebase, commit history, and development logs, the final project contribution ratio is determined to be:

**[ STUDENT: 65% ] | [ AI: 35% ]**

### Justification of Split:
- **Student Contribution (65%):**
    - **Core Decision Making:** Designed the fundamental app architecture, state management patterns, and UX flow.
    - **Integration & Refinement:** Manually reviewed, debugged, and integrated every AI-generated snippet to fit the project's specific constraints.
    - **Business Logic:** Wrote the majority of the data structure logic (LinkedList, Trie) and handled complex interaction states that the AI could not fully automate.
    - **Validation:** Conducted extensive manual testing and compiled the final production-ready binaries.
- **AI Contribution (35%):**
    - **Efficiency Boilerplate:** Accelerated development by generating standard modern C++ templates (smart pointers, structured data).
    - **Mathematical Utility:** Provided geometry and coordinate math for SFML rendering.
    - **API Navigation:** Assisted in translating legacy SFML 2.x patterns to the new SFML 3.x API.
    - **Pattern Suggestions:** Proposed higher-level design patterns (like the Fluent Builder) which were then customized and implemented by the student.

